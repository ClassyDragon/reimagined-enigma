
#pragma once
#include <iostream>
#include <vector>
#include <SFML/Graphics.hpp>

class Tetramino {
    public:
        // Default Constructor:
        Tetramino();

        // Constructor with type:
        Tetramino(char type);

        // Default Destructor:
        ~Tetramino();

    protected:
};
